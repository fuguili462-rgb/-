<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbaizhan.farm_system.mapper.UserMapper">


    <!--删除用户操作，删除用户的同时要把用户关联的角色也删除-->
    <delete id="deleteUserRolesByUserIds">
        delete from sys_user_role where user_id in
--                 collection 的值取决于传入的参数类型
        <foreach item="userId" collection="userIds" open="(" separator="," close=")">
                   #{userId}
        </foreach>
    </delete>
<!--根据用户id查询角色id-->
    <select id="selectRoleIdsByUserIds" resultType="java.lang.Long">
        select role_id from sys_user_role where user_id = #{userId}
    </select>
<!--修改用户角色sql语句，修改用户角色是把原来旧角色删除然后再添加新角色-->
    <insert id="insertUserRole">
        insert into sys_user_role(user_id,role_id)
        values
            <foreach collection="roleIds" item="roleId" separator=",">
                (#{userId},#{roleId})
            </foreach>
    </insert>
</mapper>