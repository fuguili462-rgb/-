<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbaizhan.farm_system.mapper.RoleMapper">


    <!--根据角色id，在角色权限中间表中查询权限id-->
    <select id="selectRolePermissionIds" resultType="java.lang.Long">
        select permission_id from sys_role_permission where role_id = #{roleId}
    </select>

    <!--   删除角色与权限关联 -->
    <delete id="deleteRolePermissionByRoleIds">
        delete from sys_role_permission where role_id in
            <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
                #{roleId}
            </foreach>
    </delete>
<!--    根据角色id，删除sys_user_role里面的数据-->
    <delete id="deleteUserRolesByRoleIds">
        delete from sys_user_role where role_id in
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </delete>
<!--    批量选择用户取消角色-->
    <delete id="deleteUserRolesByRoleIdAndUserIds">
        delete from sys_user_role where role_id = #{roleId} and user_id in
            <foreach collection="userIds" item="userId" open="(" separator="," close=")">
                #{userId}
            </foreach>
    </delete>
    <!--  在用户角色中间表中根据角色id查询用户id  -->
    <select id="selectUserRoleByRoleId" resultType="java.lang.Long">
        select user_id from sys_user_role where role_id = #{roleId}
    </select>
<!--  查询角色用户关联是否存在  -->
    <select id="countUserRoleExists" resultType="java.lang.Integer">
        select count(*) from sys_user_role where role_id = #{roleId} and user_id = #{userId}
    </select>
<!--   插入角色用户关联 -->
    <insert id="insertUserRole">
        insert into sys_user_role (user_id,role_id) values (#{userId},{roleId})
    </insert>
<!--   插入角色权限关联 -->
    <insert id="insertRolePermission">
        insert into sys_role_permission (role_id,permission_id) values
            <foreach collection="permissionIds" item="permissionId" separator=",">
                (#{roleId},#{permissionId})
            </foreach>
    </insert>
</mapper>